# DatingAppVue

Це front-end верстка (markup/layout) для прототипу мобільного додатку знайомств **"Fated"**. Проект демонструє ключові екрани, з якими взаємодіє користувач:
* Серія з трьох екранів онбордінгу (знайомство з додатком).
* Сторінка автентифікації з перемиканням між формами входу (Login) та реєстрації (Register).

---

## 🚀 Особливості

* **Багато-кроковий онбордінг:** Три послідовні екрани (`index.html`, `onboarding2.html`, `onboarding3.html`), які презентують додаток.
* **Плавні анімації:** Реалізовані кастомні анімації переходів між сторінками (`slide-in`/`slide-out`) та ефект `fade-in` при завантаженні.
* **Сторінка автентифікації:** Єдина сторінка `auth.html` з двома формами та анімованим перемиканням між вкладками "Login" та "Register".
* **Адаптивний дизайн:** Інтерфейс стилізований для мобільних пристроїв, імітуючи рамку смартфону, та коректно виглядає на малих екранах.
* **Кастомні шрифти:** Використання фірмових шрифтів "Lato" та "Bomber Balloon".

---
## 🏗️ Структура проекту
```
src/
├── assets/                 # Статичні ресурси
│   ├── css/                # Стилі
│   └── img/                # Зображення
│
├──components/             # Компоненти
│   ├── auth/               # Компоненти авторизації
│   │   ├── AuthHeader.vue   # Шапка авторизації з логотипом
│   │   ├── AuthTabs.vue     # Перемикач Login/Register
│   │   ├── LoginForm.vue    # Форма входу
│   │   └── RegisterForm.vue # Форма реєстрації
│   │
│   ├── layout/             # Компоненти макету
│   │   └── TheFrame.vue     # Основний контейнер застосунку
│   │
│   ├── onboarding/         # Компоненти онбордингу
│   │   ├── OnboardingContent.vue      # Контент слайду
│   │   ├── OnboardingNavigation.vue   # Навігація (Skip/Next)
│   │   └── OnboardingPagination.vue   # Пагінація (точки)
│   │
│   └── ui/                 # Базові UI компоненти
│       ├── BaseButton.vue   # Кнопка
│       └── BaseInput.vue    # Поле вводу
│
├── router/                 # Налаштування маршрутизації
│   └── index.ts             # Конфігурація Vue Router
│
└── views/                  # Сторінки застосунку
├── AuthView.vue         # Сторінка авторизації
└── OnboardingView.vue   # Сторінка онбордингу
```

## ⚙️ Налаштування конфігурації

Дивіться [Vite Configuration Reference](https://vite.dev/config/).

## 🛠️ Встановлення проекту

```sh
  npm install
```

### Компіляція та гаряче перезавантаження для розробки

```sh
  npm run dev
```

Застосунок буде доступний за адресою `http://localhost:5173`

### Перевірка типів, компіляція та мініфікація для production

```sh
  npm run build
```

### Лінтинг з [ESLint](https://eslint.org/)

```sh
  npm run lint
```

## 🎨 Технології

- **Vue 3** — прогресивний JavaScript фреймворк
- **TypeScript** — типізований JavaScript
- **Vite** — швидкий інструмент збірки
- **Vue Router** — офіційний роутер для Vue.js
- **Tailwind CSS** — utility-first CSS фреймворк

## 📱 Особливості

- ✨ Адаптивний дизайн для мобільних пристроїв \
- 🔐 Авторизація з формами входу та реєстрації
- 📖 Багатокроковий онбординг
- 🎭 Плавні переходи між сторінками
- 🧩 Модульна компонентна архітектура

