# Fated - Верстка Прототипу Додатку для Знайомств

Це front-end верстка (markup/layout) для прототипу мобільного додатку знайомств **"Fated"**. Проект демонструє ключові екрани, з якими взаємодіє користувач:
* Серія з трьох екранів онбордінгу (знайомство з додатком).
* Сторінка автентифікації з перемиканням між формами входу (Login) та реєстрації (Register).

---

## 🚀 Особливості

* **Багато-кроковий онбордінг:** Три послідовні екрани (`index.html`, `onboarding2.html`, `onboarding3.html`), які презентують додаток.
* **Плавні анімації:** Реалізовані кастомні анімації переходів між сторінками (`slide-in`/`slide-out`) та ефект `fade-in` при завантаженні.
* **Сторінка автентифікації:** Єдина сторінка `auth.html` з двома формами та анімованим перемиканням між вкладками "Login" та "Register".
* **Адаптивний дизайн:** Інтерфейс стилізований для мобільних пристроїв, імітуючи рамку смартфону, та коректно виглядає на малих екранах.
* **Кастомні шрифти:** Використання фірмових шрифтів "Lato" та "Bomber Balloon".

---

## 🛠️ Стек Технологій

Репозиторій містить **дві версії** верстки:

### Версія на Vanilla CSS/JS (Основна директорія)

Класичний підхід без використання сучасних фреймворків чи інструментів збірки.

* **HTML5**
* **CSS3:** Всі стилі написані вручну у файлі `style.css`.
* **JavaScript (ES6+):** Вся інтерактивність (анімації, перемикання форм) реалізована на "чистому" JavaScript у файлах `animations.js` та `auth.js`.

---

## 📂 Структура Проекту

    **Версія 1: Vanilla (Ванільний) CSS/JS**
        * `index.html` (Онбордінг 1)
        * `onboarding2.html`
        * `onboarding3.html`
        * `auth.html` (Автентифікація)
        * `style.css` (Основний файл стилів)
        * `animations.js` (Логіка анімацій)
        * `auth.js` (Логіка форм)
        * `fonts/` (Шрифти, вкл. `bomber-balloon.ttf`)
        * `img/` (Зображення)
---

## 🏁 Як Запустити
### Vanilla CSS/JS

Для перегляду ванільної версії просто відкрийте будь-який `.html` файл (наприклад, `index.html`) з **кореневої** директорії у вашому веб-браузері.

> **Примітка:** Для коректної роботи анімацій переходів між сторінками, вам може знадобитись запустити файли через локальний сервер (наприклад, за допомогою розширення **Live Server** у VS Code).
