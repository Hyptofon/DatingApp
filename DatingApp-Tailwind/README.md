# Fated - Верстка Прототипу Додатку для Знайомств

Це front-end верстка (markup/layout) для прототипу мобільного додатку знайомств **"Fated"**. Проект демонструє ключові екрани, з якими взаємодіє користувач:
* Серія з трьох екранів онбордінгу (знайомство з додатком).
* Сторінка автентифікації з перемиканням між формами входу (Login) та реєстрації (Register).

---

## 🚀 Особливості

* **Багато-кроковий онбордінг:** Три послідовні екрани (`index.html`, `onboarding2.html`, `onboarding3.html`), які презентують додаток.
* **Плавні анімації:** Реалізовані кастомні анімації переходів між сторінками (`slide-in`/`slide-out`) та ефект `fade-in` при завантаженні.
* **Сторінка автентифікації:** Єдина сторінка `auth.html` з двома формами та анімованим перемиканням між вкладками "Login" та "Register".
* **Адаптивний дизайн:** Інтерфейс стилізований для мобільних пристроїв, імітуючи рамку смартфону, та коректно виглядає на малих екранах.
* **Кастомні шрифти:** Використання фірмових шрифтів "Lato" та "Bomber Balloon".

---

## 🛠️ Стек Технологій
### Версія на Vite + Tailwind CSS (Директорія `DatingApp-Tailwind/`)

Сучасний підхід з використанням інструменту збірки Vite та utility-first CSS фреймворку Tailwind.

* [**Vite**](https://vitejs.dev/): Швидкий сервер для розробки та збірки проекту.
* [**Tailwind CSS**](https://tailwindcss.com/): Utility-first CSS фреймворк, що дозволяє писати стилі безпосередньо в HTML.
* **PostCSS:** Використовується для обробки Tailwind CSS та додавання автопрефіксів.
* **JavaScript (ES Modules):** Скріпти підключені як модулі (`type="module"`).

---

## 📂 Структура Проекту
    **Версія Vite + Tailwind CSS**
        * `DatingApp-Tailwind/`
            * `index.html` (Онбордінг 1 - Tailwind)
            * `onboarding2.html`
            * `onboarding3.html`
            * `auth.html` (Автентифікація - Tailwind)
            * `src/` (Вихідні файли)
                * `animations.js`
                * `auth.js`
                * `style.css` (CSS з директивами Tailwind)
            * `public/` (Статичні ресурси)
                * `fonts/`
                * `img/`
            * `package.json` (NPM залежності)
            * `vite.config.js` (Конфігурація Vite)

---

## 🏁 Як Запустити

### Версія  Vite + Tailwind CSS

Цей варіант потребує встановленого [Node.js](https://nodejs.org/) (рекомендована версія v18+).

1.  **Перейдіть у директорію проекту:**
    ```bash
    cd DatingApp-Tailwind
    ```

2.  **Встановіть залежності:**
    ```bash
    npm install
    ```

3.  **Запустіть сервер для розробки:**
    ```bash
    npm run dev
    ```


4.  Відкрийте [http://localhost:3000](http://localhost:3000) у вашому браузері (порт 3000 вказаний у `vite.config.js`).

### Збірка проекту (Build)

Для створення оптимізованої версії (Vite/Tailwind) для продакшену, виконайте:
```bash
  npm run build
```